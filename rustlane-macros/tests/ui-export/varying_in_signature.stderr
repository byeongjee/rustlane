error: `Varying` cannot appear in an #[rustlane::export] signature: its in-memory width is chosen by the runtime target dispatcher, so it cannot cross the export boundary. Take scalars / `&[T]` / `&mut [T]` and iterate with `foreach!` inside the body (each lane's varying data stays within one target's width).
  --> tests/ui-export/varying_in_signature.rs:11:11
   |
11 | fn bad(x: Varying<f32>, out: &mut [f32]) {
   |           ^^^^^^^
